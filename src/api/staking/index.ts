import { stake } from './stake'
import { canStake } from './canStake'
import { canUnstake } from './canUnstake'
import { queryCertificate } from './queryCertificate'

export default {
  stake,
  canStake,
  canUnstake,
  queryCertificate,
}
